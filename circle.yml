machine:
  timezone:
    Asia/Tokyo
  services:
    - docker
test:
  post:
    - docker build -t ruby-rpm .
    - docker run -v ruby-rpm
    - id=$( docker run -d ruby-rpm )
    - docker cp $id:/var/tmp/rpmbuild/RPMS/x86_64/* $CIRCLE_ARTIFACTS
general:
  artifacts:
    - "*.rpm"
