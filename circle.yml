machine:
  timezone:
    Asia/Tokyo
  services:
    - docker
test:
  post:
    - docker build -t sugi/ruby-rpm .
    - docker run -v $PWD:/shared:rw sugi/ruby-rpm /bin/sh ./rubybuild.sh
general:
  artifacts:
    - /shared/*
